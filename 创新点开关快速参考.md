# 三个创新点开关快速参考表

## 📍 开关位置

**配置文件**：`configs/umc_MIntRec.py`  
**方法**：`_get_ablation_config()` (第 106-209 行)  
**默认配置**：`default_config` 字典 (第 109-145 行)

---

## 🎯 创新点一：ConFEDE双投影机制

| 开关参数 | 默认值 | 位置 | 作用 |
|---------|--------|------|------|
| `enable_video_dual` | `True` | 第 112 行 | 控制视频双投影（主要信息+环境信息） |
| `enable_audio_dual` | `True` | 第 113 行 | 控制音频双投影（主要信息+环境信息） |
| `enable_dual_projection` | `True` | 第 111 行 | 双投影总开关（备用） |

**禁用方法**：
```python
# 在 _get_ablation_config() 中
'enable_video_dual': False,
'enable_audio_dual': False,
```

**命令行运行**：
```bash
python run.py --dataset MIntRec --ablation_experiment no_dual_projection --train
```

---

## 🎯 创新点二：文本引导多模态注意力融合

| 开关参数 | 默认值 | 位置 | 作用 |
|---------|--------|------|------|
| `enable_text_guided_attention` | `True` | 第 116 行 | 控制文本引导的交叉注意力 |
| `enable_self_attention` | `True` | 第 117 行 | 控制自注意力机制 |
| `self_attention_layers` | `2` | 第 118 行 | 自注意力层数 |

**禁用方法**：
```python
# 在 _get_ablation_config() 中
'enable_text_guided_attention': False,
'enable_self_attention': False,
```

**命令行运行**：
```bash
# 需要先在配置文件中添加对应的实验配置
python run.py --dataset MIntRec --ablation_experiment no_text_guided --train
```

---

## 🎯 创新点三：自适应渐进式学习策略

### 3.1 渐进式学习相关

| 开关参数 | 默认值 | 位置 | 作用 |
|---------|--------|------|------|
| `enable_progressive_learning` | `True` | 第 127 行 | 渐进式学习总开关 |
| `enable_adaptive_threshold` | `True` | 第 128 行 | 自适应阈值调整 |
| `enable_performance_monitoring` | `True` | 第 129 行 | 性能监控 |
| `enable_early_stop` | `True` | 第 130 行 | 早停机制 |

### 3.2 聚类优化相关

| 开关参数 | 默认值 | 位置 | 作用 |
|---------|--------|------|------|
| `enable_clustering_optimization` | `True` | 第 135 行 | 聚类优化总开关 |
| `enable_clustering_loss` | `True` | 第 121 行 | 聚类损失 |
| `enable_contrastive_loss` | `True` | 第 122 行 | 对比学习损失 |
| `enable_compactness_loss` | `True` | 第 123 行 | 紧密度损失 |
| `enable_separation_loss` | `True` | 第 124 行 | 分离度损失 |

**禁用方法**：
```python
# 禁用渐进式学习
'enable_progressive_learning': False,
'enable_adaptive_threshold': False,
'enable_early_stop': False,

# 禁用聚类优化
'enable_clustering_optimization': False,
'enable_clustering_loss': False,
'enable_contrastive_loss': False,
```

**命令行运行**：
```bash
# 禁用渐进式学习
python run.py --dataset MIntRec --ablation_experiment no_progressive_learning --train

# 禁用聚类损失
python run.py --dataset MIntRec --ablation_experiment no_clustering_loss --train
```

---

## 🔧 如何修改开关

### 方法1：通过命令行参数（推荐）

使用 `--ablation_experiment` 参数，系统会自动应用预定义的配置：

```bash
python run.py --dataset MIntRec --ablation_experiment no_dual_projection --train
```

### 方法2：直接修改配置文件

编辑 `configs/umc_MIntRec.py`，在 `_get_ablation_config()` 方法中：

```python
def _get_ablation_config(self, args):
    default_config = {
        # 修改这里的值
        'enable_video_dual': False,  # 改为 False 禁用
        'enable_audio_dual': False,   # 改为 False 禁用
        # ...
    }
```

### 方法3：添加自定义消融实验

在 `_get_ablation_config()` 方法中添加新的实验配置：

```python
if hasattr(args, 'ablation_experiment') and args.ablation_experiment:
    experiment_name = args.ablation_experiment.lower()
    
    # 添加您的自定义实验
    if experiment_name == 'my_experiment':
        config = default_config.copy()
        config.update({
            'enable_video_dual': False,
            'enable_audio_dual': False,
            # ... 其他配置
        })
        return config
```

---

## 📋 预定义的消融实验

### 已实现的实验

| 实验名称 | 说明 | 创新点一 | 创新点二 | 创新点三 |
|---------|------|---------|---------|---------|
| `no_dual_projection` | 禁用双投影 | ❌ | ✅ | ✅ |
| `no_clustering_loss` | 禁用聚类损失 | ✅ | ✅ | ❌ |
| `no_progressive_learning` | 禁用渐进式学习 | ✅ | ✅ | ❌ |
| `full_confede` | 完整ConFEDE | ✅ | ✅ | ✅ |

### 需要添加的实验

以下实验需要在配置文件中添加：

| 实验名称 | 说明 | 需要添加 |
|---------|------|---------|
| `only_innovation1` | 仅创新点一 | ✅ |
| `only_innovation2` | 仅创新点二 | ✅ |
| `only_innovation3` | 仅创新点三 | ✅ |
| `baseline` | 基线（禁用所有） | ✅ |

---

## 🚀 快速运行示例

### 运行完整UMC（所有创新点）
```bash
python run.py --dataset MIntRec --train --save_results
```

### 运行基线（禁用所有创新点）
```bash
# 需要先在配置文件中添加 baseline 实验配置
python run.py --dataset MIntRec --ablation_experiment baseline --train --save_results
```

### 运行单创新点消融
```bash
# 禁用创新点一
python run.py --dataset MIntRec --ablation_experiment no_dual_projection --train

# 禁用创新点三（渐进式学习）
python run.py --dataset MIntRec --ablation_experiment no_progressive_learning --train
```

### 运行多次取平均（推荐）
```bash
for seed in 0 1 2 3 4; do
    python run.py \
        --dataset MIntRec \
        --ablation_experiment no_dual_projection \
        --seed $seed \
        --train \
        --save_results
done
```

---

## 📍 代码中的实际使用位置

### 创新点一使用位置

**文件**：`backbones/FusionNets/UMC.py`

```python
# 第 305 行
if self.enable_video_dual:
    video_feat = self.video_dual_projector(video_feat)

# 第 309 行
if self.enable_audio_dual:
    audio_feat = self.audio_dual_projector(audio_feat)
```

### 创新点二使用位置

**文件**：`backbones/FusionNets/UMC.py`

```python
# 第 472 行
if self.enable_text_guided_attention:
    # 文本引导注意力计算
    ...
```

### 创新点三使用位置

**文件**：`backbones/FusionNets/UMC.py`

```python
# 第 552 行
if self.enable_clustering_optimization:
    # 聚类优化路径
    ...
```

---

## ✅ 验证开关是否生效

### 检查方法1：查看训练日志

运行时会打印配置信息，查找：
```
enable_video_dual: True/False
enable_audio_dual: True/False
enable_text_guided_attention: True/False
enable_clustering_optimization: True/False
```

### 检查方法2：查看模型代码执行

在 `backbones/FusionNets/UMC.py` 中，如果开关为 `False`，对应的 `if` 语句块不会执行。

### 检查方法3：对比实验结果

- 禁用创新点一：性能应该下降
- 禁用创新点二：性能应该下降
- 禁用创新点三：性能应该下降

---

## 📚 相关文档

- `MIntRec消融实验指南.md` - 详细的消融实验指南
- `ABLATION_EXPERIMENTS_README.md` - 消融实验说明
- `configs/umc_MIntRec.py` - 配置文件（开关定义位置）
- `backbones/FusionNets/UMC.py` - 模型实现（开关使用位置）

---

**快速参考**：所有开关都在 `configs/umc_MIntRec.py` 的 `_get_ablation_config()` 方法中定义！


